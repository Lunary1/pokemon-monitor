{
  "name": "@pokemon-monitor/monitor-worker",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "echo No source files to build yet",
    "start": "tsx src/index.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "echo No source files to typecheck yet"
  },
  "dependencies": {
    "@pokemon-monitor/core": "workspace:*",
    "@pokemon-monitor/db": "workspace:*",
    "@pokemon-monitor/notifications": "workspace:*",
    "@pokemon-monitor/store-adapters": "workspace:*",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.2"
  }
}
